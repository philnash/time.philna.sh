{
  "name": "timetime.zone",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "preview:e2e": "vite preview --host 127.0.0.1 --port 4173 --strictPort",
    "start": "npm run dev",
    "build:cities": "node scripts/build-cities.js",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui",
    "test": "node --check src/app.js && node --check public/sw.js && node --check scripts/build-cities.js && node --check vite.config.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "24.x"
  },
  "type": "commonjs",
  "dependencies": {
    "city-timezones": "^1.3.3",
    "d3-geo": "^3.1.1",
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "vite": "^6.4.1"
  }
}
